function AutoAureus()
    local LocalPlayer = game.Players.LocalPlayer
    local RootPlace = game:GetService("Workspace").Buildings.asdqweasdqweasdqwq["Scavenge Station"].Union
    local RootPart = game:GetService('Workspace').Drones.LocalPlayer.Hull
    local pos = RootPlace.CFrame

        [RootPart].CFrame = game:GetService('Workspace').DroneShipment.MeshPart.CFrame

    wait(1)

        game:GetService("ReplicatedStorage").Events.MenuAcitonEvent:FireServer(3) then
         wait(0.1) do
        [RootPart].CFrame = game:GetService("Workspace").Buildings.asdqweasdqweasdqwq["Scavenge Station"].Union.CFrame

        end
    wait(1)

        game:GetService("ReplicatedStorage").Events.MenuAcitonEvent:FireServer(4)
    end
end
function BackWard(Buildings)
    if game:GetService("Workspace").Buildings.LocalPlayer."Scavenge Station".Union.FindFirstChild(Buildings)
    
